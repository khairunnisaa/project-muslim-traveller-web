import React from 'react';
import {Box, Typography} from "@mui/material";

const Destination = () => (
    <Box>
        <Typography>Destination Page</Typography>
    </Box>
);

export default Destination;
